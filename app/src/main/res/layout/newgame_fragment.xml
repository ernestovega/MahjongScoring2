<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:background="@android:color/white"
    tools:context=".app.main.new_game.NewGameFragment">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbarNewGame"
        android:layout_width="match_parent"
        android:layout_height="?android:attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:elevation="@dimen/toolbar_elevation"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        app:title="@string/new_game"
        tools:ignore="UnusedAttribute"/>

    <com.pchmn.materialchips.ChipsInput
        android:id="@+id/chipsInputNewGame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?android:attr/actionBarSize"
        android:animateLayoutChanges="true"
        android:fitsSystemWindows="true"
        android:padding="@dimen/standard_margin"
        app:chip_backgroundColor="@color/colorPrimary"
        app:chip_deletable="true"
        app:chip_deleteIconColor="@android:color/white"
        app:chip_labelColor="@android:color/white"
        app:hint="@string/enter_a_name"
        app:maxRows="3"
        app:showChipDetailed="false"/>

    <com.github.clans.fab.FloatingActionButton
        android:id="@+id/fabNewGameStartGame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end|bottom"
        android:layout_margin="@dimen/standard_margin"
        android:src="@drawable/ic_play_arrow_white_24px"
        android:tint="@android:color/white"
        app:fab_colorNormal="@color/colorAccent"
        app:fab_colorPressed="@color/colorAccentDark"
        app:fab_colorRipple="@color/colorAccentDark"
        android:visibility="gone"
        app:fabSize="normal"/>

    <LinearLayout
        android:id="@+id/llProgress"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:background="@color/grayMMTransparent"
        android:gravity="center"
        android:visibility="gone"
        tools:visibility="visible">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="TODO" />

    </LinearLayout>

</android.support.design.widget.CoordinatorLayout>